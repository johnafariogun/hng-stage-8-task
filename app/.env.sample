# OpenRouter API Configuration
OPENROUTER_API_KEY=sk-or-v1-1f8a7-router-kewy

# S3/MinIO Configuration (for local development, these are already set in docker-compose)
S3_BUCKET=document-processor
AWS_REGION=us-east-1
S3_ENDPOINT=http://localhost:9000
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin

# For production AWS S3, comment out S3_ENDPOINT and use:
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
